# NetworkDeploymentTool — install with: pip install -r requirements.txt
# Python >= 3.9 recommended (matches existing server venv)

# --- Server API (server/) ---
Flask==2.1.3
Flask-Cors==3.0.10
pymongo==4.2.0

# --- Device bundles (device_bundles/) ---
netmiko>=4.1.0
requests>=2.28.0

# --- Sandbox (server/sandbox/) ---
gns3fy>=0.8.0
docker>=6.0.0

# --- Unit test tooling (SandboxInternalTestController/, testcases/) ---
pyats[full]>=23.0
